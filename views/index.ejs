<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10" >
            <form action="create" method="post">
                <input type="text" placeholder="Title your task" class=" w-full rounded-md text-white  outline-none block px-5 py-2 bg-zinc-700" name="title">
                <textarea placeholder="Enter your task details"  class="w-full rounded-md text-white block mt-2 resize-none px-5 py-2 bg-zinc-700" name="details"></textarea>
                <input type="submit" value="Add Task"  class="rounded-md text-white block mt-2 px-5 py-2 bg-slate-800">
            </form>
        </div>
        <div class="task p-10 flex gap-3 flex-wrap">
            <% if(files.length>0){%>
                <% files.forEach(function(val){%>
                    <div class="task w-72  px-3 py-4 rounded-md bg-zinc-800 text-white " >
                        <p><%= val %></p>
                        <a href="/files/<%=val%>" class="text-blue-400 inline-block mt-3">Read more</a>
                    </div>
                <%})%>
            <%} else {%>
                <h3 class="text-zinc-700">No task yet.</h3>
            <% } %>
        </div>
    </div>
</body>
</html>